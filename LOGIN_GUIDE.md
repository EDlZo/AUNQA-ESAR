# คู่มือการเข้าสู่ระบบ AUNQA Assessment System

## ข้อมูลสำหรับการเข้าสู่ระบบ (ตรงกับฐานข้อมูล)

### Admin
- **Username**: `admin@example.com`
- **Password**: `adminpass`
- **Role**: Admin
- **สิทธิ์**: อ่าน เขียน ลบ และจัดการระบบได้

### Staff
- **Username**: `staff@example.com`
- **Password**: `staffpass`
- **Role**: Staff
- **สิทธิ์**: อ่านและเขียนข้อมูลได้

### Evaluator
- **Username**: `evaluator@example.com`
- **Password**: `evaluatorpass`
- **Role**: Evaluator
- **สิทธิ์**: อ่านและเขียนข้อมูลได้

### External Evaluator
- **Username**: `external@example.com`
- **Password**: `externalpass`
- **Role**: External Evaluator
- **สิทธิ์**: อ่านและเขียนข้อมูลได้

### Developer
- **Username**: `dev@example.com`
- **Password**: `devpass`
- **Role**: Developer
- **สิทธิ์**: อ่าน เขียน ลบ และจัดการระบบได้ (เข้าถึงทุกหน้า)

## วิธีการใช้งาน

1. **เปิดเว็บไซต์** - เปิดเบราว์เซอร์และไปที่ `http://localhost:5173`
2. **คลิกปุ่ม "เข้าสู่ระบบ"** - ที่มุมขวาบนของหน้าเว็บ
3. **กรอกข้อมูล** - ใส่ Username, Password และเลือก Role ที่ตรงกับข้อมูลในฐานข้อมูล
4. **กดปุ่ม "เข้าสู่ระบบ"** - ระบบจะตรวจสอบและพาคุณไปยังหน้า Dashboard ที่เหมาะสม

## การทำงานของระบบ

### 🔐 **การตรวจสอบการเข้าสู่ระบบ:**
- ระบบจะตรวจสอบ Username และ Password ในฐานข้อมูล
- ตรวจสอบว่า Role ที่เลือกตรงกับข้อมูลในฐานข้อมูลหรือไม่
- ถ้าข้อมูลถูกต้อง จะเข้าสู่ระบบและพาไปหน้า Dashboard

### 🎯 **การนำทางตาม Role:**
- **Admin** → หน้า Define Component (จัดการองค์ประกอบคุณภาพ)
- **Staff** → หน้า Manage Component (จัดการองค์ประกอบ)
- **Evaluator** → หน้า Report + Results (รายงานและผลลัพธ์)
- **External Evaluator** → หน้า Results (ผลลัพธ์เท่านั้น)
- **Developer** → ทุกหน้า (Define Component, Manage Component, Report, Results)

## หมายเหตุ

- ระบบนี้เชื่อมต่อกับฐานข้อมูล MySQL จริง
- ข้อมูลผู้ใช้ทั้งหมดมาจากฐานข้อมูล
- หากมีปัญหาในการเข้าสู่ระบบ กรุณาตรวจสอบข้อมูลที่กรอกให้ถูกต้อง 