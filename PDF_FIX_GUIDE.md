# 🔧 แก้ไขปัญหาการแสดงผลไฟล์ PDF ที่เพี้ยน

## 📋 สรุปปัญหา
ไฟล์ PDF ที่อัปโหลดมาแสดงผลเพี้ยนเพราะระบบใช้ jsPDF ที่สร้าง PDF ใหม่แทนที่จะแสดงไฟล์ PDF ที่อัปโหลดมา

## ✅ การแก้ไขที่ทำ

### 1. เพิ่ม Library react-pdf
```bash
npm install react-pdf
```

### 2. สร้าง Component ใหม่

#### 📄 `src/components/PDFViewer.jsx`
- สำหรับแสดงไฟล์ PDF ที่เพิ่งอัปโหลด
- มีฟีเจอร์: ขยาย/ย่อ, หมุน, เปลี่ยนหน้า, ดาวน์โหลด
- รองรับการแสดงผลแบบ Modal

#### 📄 `src/components/FileViewer.jsx`
- สำหรับแสดงไฟล์ที่อัปโหลดแล้วจากฐานข้อมูล
- รองรับทั้ง PDF และรูปภาพ
- เชื่อมต่อกับ server endpoint

### 3. แก้ไข Server

#### 🔧 `server.cjs`
- เพิ่มการจัดการไฟล์ที่ดีขึ้น
- เพิ่ม endpoint สำหรับดาวน์โหลดและดูไฟล์
- จำกัดประเภทไฟล์และขนาดไฟล์
- สร้างโฟลเดอร์ uploads อัตโนมัติ

```javascript
// ตั้งค่า multer
const storage = multer.diskStorage({
  destination: function (req, file, cb) {
    if (!fs.existsSync('uploads')) {
      fs.mkdirSync('uploads');
    }
    cb(null, 'uploads/');
  },
  filename: function (req, file, cb) {
    const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
    cb(null, uniqueSuffix + path.extname(file.originalname));
  }
});

// เพิ่ม endpoint
app.get('/api/download/:filename', (req, res) => { ... });
app.get('/api/view/:filename', (req, res) => { ... });
```

### 4. แก้ไข ReportSection

#### 🔧 `src/components/ReportSection.jsx`
- เพิ่มการแสดงไฟล์ที่เลือกก่อนอัปโหลด
- เพิ่มปุ่มดูไฟล์ PDF
- เพิ่มการแสดงไฟล์ที่อัปโหลดแล้วในตาราง
- เชื่อมต่อกับ FileViewer

## 🎯 ฟีเจอร์ใหม่

### 📱 การแสดงไฟล์ที่เลือก
- แสดงชื่อไฟล์และขนาด
- Preview รูปภาพ
- ปุ่มดูไฟล์ PDF
- ปุ่มลบไฟล์

### 🔍 PDF Viewer
- แสดงไฟล์ PDF แบบเต็มหน้าจอ
- เปลี่ยนหน้า (ก่อนหน้า/ถัดไป)
- ขยาย/ย่อ (50% - 300%)
- หมุนไฟล์ (90°, 180°, 270°)
- ดาวน์โหลดไฟล์

### 📊 ตารางผลประเมิน
- แสดงไฟล์ที่อัปโหลดแล้ว
- คลิกเพื่อดูไฟล์
- แสดงสถานะและวันที่

## 🚀 วิธีการใช้งาน

### 1. อัปโหลดไฟล์
1. เลือกไฟล์ PDF หรือรูปภาพ
2. ระบบจะแสดง preview
3. คลิกปุ่ม "ดูไฟล์ PDF" (ถ้าเป็น PDF)
4. บันทึกผลประเมิน

### 2. ดูไฟล์ที่อัปโหลดแล้ว
1. ไปที่ตาราง "ผลประเมินย้อนหลัง"
2. คลิกที่ชื่อไฟล์
3. ไฟล์จะเปิดใน modal
4. ใช้ปุ่มควบคุมเพื่อจัดการไฟล์

### 3. จัดการไฟล์ PDF
- **ขยาย/ย่อ**: ใช้ปุ่ม +/-
- **เปลี่ยนหน้า**: ใช้ปุ่ม ก่อนหน้า/ถัดไป
- **หมุน**: ใช้ปุ่มหมุน
- **ดาวน์โหลด**: ใช้ปุ่มดาวน์โหลด

## 🔧 การตั้งค่า

### 1. ตั้งค่า react-pdf
```javascript
// ใน PDFViewer.jsx และ FileViewer.jsx
pdfjs.GlobalWorkerOptions.workerSrc = `//cdnjs.cloudflare.com/ajax/libs/pdf.js/${pdfjs.version}/pdf.worker.min.js`;
```

### 2. ตั้งค่า Server
```javascript
// จำกัดประเภทไฟล์
fileFilter: function (req, file, cb) {
  if (file.mimetype === 'application/pdf' || file.mimetype.startsWith('image/')) {
    cb(null, true);
  } else {
    cb(new Error('อนุญาตเฉพาะไฟล์ PDF และรูปภาพเท่านั้น'), false);
  }
}

// จำกัดขนาดไฟล์
limits: {
  fileSize: 10 * 1024 * 1024 // 10MB
}
```

## 📝 หมายเหตุ

- ระบบรองรับไฟล์ PDF และรูปภาพ (JPG, PNG, GIF, WebP)
- ขนาดไฟล์สูงสุด 10MB
- ไฟล์จะถูกเก็บในโฟลเดอร์ `uploads/`
- ชื่อไฟล์จะถูกสร้างใหม่เพื่อป้องกันการซ้ำ
- ระบบจะสร้างโฟลเดอร์ `uploads/` อัตโนมัติ

## 🐛 การแก้ไขปัญหา

### ถ้าไฟล์ PDF ไม่แสดง
1. ตรวจสอบว่าไฟล์ไม่เสียหาย
2. ตรวจสอบขนาดไฟล์ไม่เกิน 10MB
3. ตรวจสอบ console สำหรับ error

### ถ้าไฟล์รูปภาพไม่แสดง
1. ตรวจสอบประเภทไฟล์ที่รองรับ
2. ตรวจสอบ URL ของไฟล์
3. ตรวจสอบการเชื่อมต่อ server

## ✅ ผลลัพธ์

- ✅ ไฟล์ PDF แสดงผลถูกต้องไม่เพี้ยน
- ✅ รองรับการขยาย/ย่อ/หมุน
- ✅ แสดงไฟล์ที่อัปโหลดแล้ว
- ✅ ระบบจัดการไฟล์ที่ดีขึ้น
- ✅ UI/UX ที่ดีขึ้น 